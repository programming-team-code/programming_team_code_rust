//! # Interval Container

use std::collections::BTreeMap;
use std::ops::Range;

pub struct IntervalContainer {
    mp: BTreeMap<i32, i32>,
}

impl IntervalContainer {
    pub fn add_interval(&mut self, range: Range<i32>) {
        while let Some(
        loop {
            self.mp.range(range.clone())
        }
    }
}
